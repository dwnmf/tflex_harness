# Semantic parametric GRB reverse output

Generated by:

```powershell
python -m tflex_harness.cli reverse-evidence agent_workspace/snippets/grb_reverse_planetary/model_evidence_with_contours.json --output-dir agent_workspace/snippets/grb_reverse_planetary/semantic_parametric
```

Files:

- `semantic_model.json` — recognized operations with confidence and parameters.
- `parametric_candidate.cs` — readable parametric C# using `TFlexEasyGears`, `EasySolids`, and `EasyExport`.

Live validation:

- run dir: `artifacts/runs/20260525_133320_520936_grb_reverse_planetary_blind_parametric_live`
- result: `exit_code=0`
- operations: `9`
- artifacts:
  - `parametric_from_grb.grb` size `619437`
  - `parametric_from_grb.step` size `1955345`

This is Phase 2: semantic reconstruction. It removes raw `double[,]` point clouds for recognized shapes and emits parameterized helper calls.
